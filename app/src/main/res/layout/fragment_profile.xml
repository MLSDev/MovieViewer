<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="android.view.View" />

        <variable
            name="viewModel"
            type="com.shykun.volodymyr.movieviewer.presentation.profile.ProfileViewModel"/>

        <variable
            name="accountDetails"
            type="com.shykun.volodymyr.movieviewer.data.network.response.AccountDetailsResponse" />
    </data>


    <android.support.design.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">


        <Button
            android:id="@+id/loginButton"
            android:layout_width="200dp"
            android:layout_height="40dp"
            android:layout_gravity="center"
            android:background="@drawable/button_rounded"
            android:text="Login"
            android:visibility="@{viewModel.sessionIdLiveData == null ? View.VISIBLE : View.GONE}" />

        <ProgressBar
            android:id="@+id/profileProgressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="@{viewModel.sessionIdLiveData != null &amp;&amp; accountDetails == null ? View.VISIBLE : View.GONE}"/>

        <android.support.design.widget.AppBarLayout
            android:id="@+id/profileAppBarLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:visibility="@{accountDetails == null ? View.GONE : View.VISIBLE}">


            <android.support.design.widget.CollapsingToolbarLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content">


                <ImageView
                    android:layout_width="match_parent"
                    android:layout_height="150dp" />

                <ImageView
                    android:id="@+id/profilePhoto"
                    android:layout_width="100dp"
                    android:layout_height="100dp"
                    android:layout_margin="16dp"
                    android:background="@drawable/round"
                    android:scaleType="centerCrop"
                    android:src="@drawable/ic_person" />

            </android.support.design.widget.CollapsingToolbarLayout>


        </android.support.design.widget.AppBarLayout>

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="@{accountDetails == null ? View.GONE : View.VISIBLE}"
            app:layout_behavior="@string/appbar_scrolling_view_behavior">

            <TextView
                android:id="@+id/userName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{accountDetails.username}"/>

        </RelativeLayout>

    </android.support.design.widget.CoordinatorLayout>

</layout>